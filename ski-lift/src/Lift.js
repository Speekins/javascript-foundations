var Skier = require('./Skier');

class Lift {
  constructor(limit) {
    this.limit = limit
    this.inService = true;
    this.skiers = [];
    this.safetyBar = 'up';
  }
  
  admitSkier(name, )
}

module.exports = Lift;